// This is how a comment is added to JavaScript
// Comments are not executed - they are just there to provide extra
// information to you or other developers

// Exercise Time!

// 1) Create three new variables:
//    - A variable that stores the name of an online course of your choice
//    - A variable that stores the price of that course
//    - A variable that stores the three main goals that you have, when taking this course
// 2) Output ("alert") the three variable values
// 3) Try "grouping" the three variables together and still output their values thereafter
// 4) Also output the second element in your "main goals" variable
// 5) Add a custom command that does the following:
//    - Use your "main goals" variable and access an element by its identifier
//    - The concrete identifier value should be dynamic / flexible
//      (i.e. the command can be executed for different identifier)
//    - The "main goals" variable should also be dynamic: The command should work
//      with ANY list of values
//    - The custom command should provide the accessed value (i.e. the list element)
// 6) Execute your custom command from (5) and output ("alert") the result

// Вот как комментарий добавляется в JavaScript
// Комментарии не выполняются — они нужны только для того, чтобы предоставить дополнительную информацию.
// информация для вас или других разработчиков

// Время выполнения упражнения!

// 1) Создайте три новые переменные:
// - Переменная, в которой хранится название онлайн-курса по вашему выбору
// - Переменная, хранящая цену этого курса
// - Переменная, в которой хранятся три основные цели, которые вы преследуете при прохождении этого курса
// 2) Вывод («предупреждение») трех значений переменных
// 3) Попробуйте «сгруппировать» три переменные вместе и после этого выводить их значения
// 4) Также выведите второй элемент в переменную «основные цели».
// 5) Добавьте пользовательскую команду, которая делает следующее:
// - Используйте переменную «основные цели» и получите доступ к элементу по его идентификатору
// - Конкретное значение идентификатора должно быть динамическим/гибким
// (т.е. команду можно выполнить для другого идентификатора)
// - Переменная "основные цели" также должна быть динамической: Команда должна работать
// с ЛЮБЫМ списком значений
// - Пользовательская команда должна предоставить доступное значение (т.е. элемент списка)
// 6) Выполните вашу пользовательскую команду из (5) и выведите («предупреждение») результат
